# Socker Node.js Demo
A minimal Node.js HTTP application containerized with Docker. This project demonstrates the fundamentals of creating a Node.js application, writing a Dockerfile, building a Docker image, running a container, exposing ports, and understanding Docker layer caching.

## Features



## Project Structure

```text
docker-node-demo/
├── index.js
├── Dockerfile
└── .dockerignore
```

## Requirements

 Nodejs


